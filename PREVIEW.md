# Landing Page Preview

Para conferir a versão atual da landing page:

1. Inicie um servidor local na raiz do projeto:
   ```bash
   python3 -m http.server 8000
   ```
2. Acesse [http://localhost:8000/](http://localhost:8000/) no navegador para carregar o `index.html`.
3. Atualize a página sempre que fizer mudanças para visualizar o resultado mais recente.

> **Dica:** Caso possua um banner em PNG enviado pelo time de design, salve-o como `hero-banner.png` na raiz do projeto.
> Se o arquivo não existir, a página exibe automaticamente a versão em SVG (`hero-banner.svg`) incluída no repositório.

Prévia estática mais recente:

![Landing Preview](browser:/invocations/doghkcei/artifacts/artifacts/landing-page-preview.png)

Capturada em 07 Nov 2025.
